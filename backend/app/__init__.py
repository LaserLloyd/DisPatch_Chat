"""DisPatch Chat — a lightweight, local-first chat UI for OpenClaw agents.

Single-user, no auth, thread-isolated. The backend manages conversation state
and display; all AI logic lives in OpenClaw (driven via the `openclaw agent` CLI).
"""

__version__ = "1.0.0"
